[project]
name = "cti-agent"
version = "0.1.0"
description = "A Cyber Threat Intelligence Agent for analyzing SBOMs."
authors = [{ name = "Your Name", email = "you@example.com" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic-ai",
    "cyclonedx-python-lib",
    "python-dotenv",
    "pytest",
    "uv",
    "requests",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
